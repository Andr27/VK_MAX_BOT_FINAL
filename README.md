# VK MAX BOT

Бот для VK с интеграцией GigaChat AI, парсером расписания и системой управления дедлайнами.

## 🚀 Возможности

- 📅 **Просмотр расписания** - автоматический парсинг расписания из различных источников (dnevuch.ru, togu и др.)
- 🤖 **Чат с GigaChat AI** - интеллектуальный помощник для учебы
- ⏰ **Управление дедлайнами** - добавление, редактирование и удаление дедлайнов через естественный язык
- 📋 **Кэширование данных** - расписание кэшируется для быстрого доступа
- 🎓 **Поддержка множества вузов** - автоматическое определение университета по названию

## 📋 Требования

### Для локального запуска:
- **Node.js** версии 18 или выше
- **Python** версии 3.9 или выше
- **npm** или **yarn**
- **pip** (менеджер пакетов Python)

### Для запуска через Docker:
- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше

## 🔧 Установка и запуск

### Вариант 1: Локальный запуск (рекомендуется для разработки)

#### Шаг 1: Клонирование репозитория
```bash
git clone <repository-url>
cd VK_MAX_BOT
```

#### Шаг 2: Установка зависимостей

**Установка Node.js зависимостей:**
```bash
npm install
```

**Установка Python зависимостей:**
```bash
pip install -r requirements.txt
```

Или установите все зависимости одной командой:
```bash
# Windows PowerShell
npm install && pip install -r requirements.txt

# Linux/Mac
npm install && pip install -r requirements.txt
```

#### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Токен бота VK (обязательно)
BOT_TOKEN=your_bot_token_here

# GigaChat API Credentials (обязательно для работы GigaChat)
# Получите Client ID и Client Secret в личном кабинете GigaChat API
# Затем объедините их в формате "Client ID:Client Secret" и закодируйте в Base64
GIGACHAT_CREDENTIALS=your_base64_encoded_credentials_here
```

**Как получить GIGACHAT_CREDENTIALS:**

1. Зарегистрируйтесь на [платформе разработчиков Сбера](https://developers.sber.ru/)
2. Создайте проект GigaChat API в личном кабинете
3. Получите `Client ID` и `Client Secret`
4. Объедините их в строку формата `Client ID:Client Secret`
5. Закодируйте эту строку в Base64:

**В Linux/Mac:**
```bash
echo -n "your_client_id:your_client_secret" | base64
```

**В Windows PowerShell:**
```powershell
[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("your_client_id:your_client_secret"))
```

**Онлайн:** Используйте любой Base64 кодировщик, например [base64encode.org](https://www.base64encode.org/)

#### Шаг 4: Сборка проекта

```bash
npm run build
```

#### Шаг 5: Запуск бота

**Режим разработки (с автоперезагрузкой):**
```bash
npm run dev
```

**Production режим:**
```bash
npm start
```

**Или одной командой (сборка + запуск):**
```bash
npm run prod
```

---

### Вариант 2: Запуск через Docker

#### Шаг 1: Подготовка

Убедитесь, что файл `.env` создан и содержит необходимые переменные (см. Шаг 3 выше).

#### Шаг 2: Сборка Docker образа

```bash
docker build -t vk-max-bot .
```

#### Шаг 3: Запуск контейнера

**Через Docker Compose (рекомендуется):**
```bash
docker-compose up -d
```

**Или через Docker напрямую:**
```bash
docker run -d \
  --name vk-max-bot \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  vk-max-bot
```

**Для Windows PowerShell:**
```powershell
docker run -d `
  --name vk-max-bot `
  --restart unless-stopped `
  --env-file .env `
  -v ${PWD}/data:/app/data `
  vk-max-bot
```

#### Шаг 4: Просмотр логов

```bash
# Через Docker Compose
docker-compose logs -f

# Через Docker
docker logs -f vk-max-bot
```

#### Шаг 5: Остановка контейнера

```bash
# Через Docker Compose
docker-compose down

# Через Docker
docker stop vk-max-bot
docker rm vk-max-bot
```

---

## 📝 Примеры команд

### Локальный запуск

```bash
# Установка всех зависимостей
npm install && pip install -r requirements.txt

# Сборка TypeScript проекта
npm run build

# Запуск в режиме разработки
npm run dev

# Запуск в production режиме
npm start
```

### Docker команды

```bash
# Сборка образа
docker build -t vk-max-bot .

# Запуск через Docker Compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f vk-max-bot

# Остановка
docker-compose down

# Пересборка и перезапуск
docker-compose up -d --build

# Очистка (удаление контейнера и образа)
docker-compose down -v
docker rmi vk-max-bot
```

---

## 📁 Структура проекта

```
VK_MAX_BOT/
├── src/                    # Исходный код TypeScript
│   ├── bot.ts             # Основной файл бота
│   ├── database/         # Работа с данными пользователей
│   ├── parser/            # Интеграция с парсером расписания
│   └── utils/             # Вспомогательные утилиты
├── parser/                # Python парсер расписания
│   ├── parser.py         # Основной скрипт парсера
│   └── requirements.txt   # Python зависимости
├── data/                  # Данные пользователей (создается автоматически)
├── dist/                  # Скомпилированный JavaScript (создается после сборки)
├── .env                   # Переменные окружения (создать вручную)
├── Dockerfile             # Конфигурация Docker образа
├── docker-compose.yml     # Конфигурация Docker Compose
├── package.json           # Node.js зависимости
├── requirements.txt       # Все зависимости проекта (Python + описание Node.js)
├── tsconfig.json          # Конфигурация TypeScript
└── README.md              # Этот файл
```

---

## 🎮 Использование бота

1. Отправьте боту команду `/start` в VK
2. Выберите действие из главного меню:
   - **📅 Расписание** - просмотр расписания (требуется настройка университета и группы при первом использовании)
   - **🤖 GigaChat** - чат с AI помощником
   - **⏰ Дедлайны** - управление дедлайнами

### Расписание

- При первом использовании бот попросит указать университет и группу
- Расписание автоматически кэшируется
- Доступен просмотр на 3 дня (вчера, сегодня, завтра) и на неделю

### GigaChat

- Нажмите кнопку "🤖 GigaChat" для активации режима чата
- Задавайте вопросы - AI поможет с учебой
- Можно добавлять дедлайны через естественный язык, например:
  - "Мне нужно сдать курсовую по математике через неделю"
  - "Дедлайн на реферат по истории завтра"
  - "Сделать домашнюю работу по физике 25.12"

### Дедлайны

- Просмотр всех активных дедлайнов
- Добавление через GigaChat или вручную
- Редактирование и удаление дедлайнов
- Автоматические напоминания о приближающихся дедлайнах

---

## 🔍 Решение проблем

### Ошибка: "BOT_TOKEN не найден"
- Убедитесь, что файл `.env` создан в корне проекта
- Проверьте, что переменная `BOT_TOKEN` указана правильно
- Убедитесь, что нет лишних пробелов в `.env` файле

### Ошибка: "GIGACHAT_CREDENTIALS не найден"
- Это предупреждение, не критическая ошибка
- Бот будет работать, но функция GigaChat будет недоступна
- Для работы GigaChat добавьте `GIGACHAT_CREDENTIALS` в `.env`

### Ошибка: "Cannot find module 'python3'"
- Убедитесь, что Python установлен и доступен в PATH
- Проверьте установку: `python --version` или `python3 --version`
- Установите Python зависимости: `pip install -r requirements.txt`

### Ошибка при парсинге расписания
- Убедитесь, что Python парсер доступен: проверьте наличие `parser/parser.py`
- Проверьте установку Python зависимостей: `pip list | grep -E "requests|beautifulsoup4"`
- Проверьте логи бота для детальной информации об ошибке

### Docker: "Cannot connect to Docker daemon"
- Убедитесь, что Docker запущен
- На Linux может потребоваться `sudo` или добавление пользователя в группу `docker`

### Docker: "Error loading .env file"
- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте синтаксис файла (нет ли лишних пробелов, правильные кавычки)

---

## 🛠️ Разработка

### Скрипты npm

- `npm run dev` - запуск в режиме разработки с автоперезагрузкой
- `npm run build` - сборка TypeScript проекта
- `npm start` - запуск скомпилированного проекта
- `npm run prod` - сборка + запуск (production)

### Структура кода

- `src/bot.ts` - основная логика бота, обработчики команд и действий
- `src/utils/gigachat.ts` - сервис для работы с GigaChat API
- `src/utils/deadlineParser.ts` - парсинг дедлайнов из естественного языка
- `src/parser/scheduleParser.ts` - интеграция с Python парсером расписания
- `src/database/userData.ts` - управление данными пользователей

---

## 📚 Документация API

- [GigaChat API](https://developers.sber.ru/docs/ru/gigachat/api/reference/rest/post-token)
- [MAX Bot API](https://github.com/maxhub/max-bot-api)

---

## 📄 Лицензия

[Укажите лицензию проекта]

---

## 👥 Авторы

[Укажите авторов проекта]

---

## 🤝 Вклад в проект

[Укажите, как можно внести вклад в проект]
